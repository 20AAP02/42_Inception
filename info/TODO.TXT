Recommended To Read:
- documentation related to Docker usage
- how daemons work and whether it’s a good idea to use them or not
- PID 1 and the best practices for writing Dockerfiles

Important Info:
- VM ip adress (192.168.56.101) - ssh amaria-m@192.168.56.101

Steps to make inside VM
- trie this command: docker version (if docker is not installed search for the command to install it; link: https://docs.docker.com/engine/install/ubuntu/)
- When you're done with the docker image, you can post it to dockerHub, so that it is accessable from any pc
- command to see present images localy: sudo docker image ls
- command to run ubuntu terminal: sudo docker run -it ubuntu 
